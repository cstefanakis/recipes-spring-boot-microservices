spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ingredient_categories
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true